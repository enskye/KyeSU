name: ClangFormat check

on:
  # Manual only: no push/PR triggers, run from the Actions tab when needed.
  workflow_dispatch:

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: check
        working-directory: kernel
        run: make check-format
